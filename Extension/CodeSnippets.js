// MARK: Code snippets.

// MARK: window.alert().
alert(message);

// MARK: window.confirm().
if (confirm(message)) {
    // code
}

// MARK: window.prompt().
let input = prompt(message, defaultValue);

// MARK: for loop.
for (let i = 0; i < 8; i++) {
    // code
}

// MARK: if...else.
if (true) {
    // code
} else {
    // code
}

// MARK: Document.querySelector().
document.querySelector(selectors);

// MARK: Document.querySelectorAll().
document.querySelectorAll(selectors);

// MARK: Document.getElementById().
document.getElementById(id);
